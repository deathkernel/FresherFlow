# Required for admin login. Set these in your local environment/deployment secrets.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=replace-with-a-strong-random-password

# Optional: required for scheduled public-job sync endpoint.
PUBLIC_JOB_SYNC_TOKEN=replace-with-a-long-random-token
